/* Template used by docker-entrypoint.sh to generate env-config.js inside
   the container from the VITE_API_URL environment variable. See
   env-config.js for the local/dev default and full explanation. */
window.__VISIONQC_API_BASE__ = "__VITE_API_URL__";
