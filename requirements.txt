# --- Web framework & server -------------------------------------------------
# NOTE on FastAPI: the project brief recommended FastAPI + SQLAlchemy. This
# was developed in a sandboxed environment with no outbound network access
# (pip/npm/apt were all blocked), so FastAPI/SQLAlchemy could not be
# installed or verified there. Flask (a mature, equally standard choice,
# explicitly allowed by the assessment brief's "another suitable framework")
# is used instead, and every endpoint/test in this repo was actually run and
# verified. See SUBMISSION_NOTES.md for the full rationale. Porting the route
# layer to FastAPI later is straightforward since business logic lives in
# app/services/ and app/database.py, independent of the web framework.
flask==3.1.3
werkzeug==3.1.3
gunicorn==23.0.0

# --- Computer vision / numerics ---------------------------------------------
opencv-python-headless==4.13.0.92
numpy==2.4.4
pillow==12.2.0
scikit-image==0.26.0

# --- Machine learning ---------------------------------------------------------
scikit-learn==1.8.0
joblib==1.5.3

# --- Training-time visualization ---------------------------------------------
matplotlib==3.10.9

# --- Testing -------------------------------------------------------------------
pytest==8.3.3
